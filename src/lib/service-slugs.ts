// Single source of truth for service page slugs — used by the
// /[locale]/diensten/[slug] route and the sitemap so they cannot drift.
export const SERVICE_SLUGS = [
  "russische-manicure",
  "biab",
  "gelnagels",
  "pedicure",
  "wimpers-lamineren",
  "wenkbrauw-laminatie",
] as const;
